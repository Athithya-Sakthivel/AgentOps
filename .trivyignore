# AWS Security Group egress to 0.0.0.0/0 is intentionally restricted to HTTPS (443 only)
# Required for:
# - AWS APIs (S3, STS, Bedrock)
# - Cloudflare Tunnel outbound connectivity
# - No NAT Gateway architecture (ADR decision)

AWS-0104